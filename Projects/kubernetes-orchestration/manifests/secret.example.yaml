# Secret (EXAMPLE ONLY — placeholder values).
# Real secrets are created at deploy time and never committed.
apiVersion: v1
kind: Secret
metadata:
  name: app-secret
type: Opaque
stringData:
  DB_USER: "changeme"
  DB_PASSWORD: "changeme"