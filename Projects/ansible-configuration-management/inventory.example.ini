# Example inventory — copy to inventory.ini and fill in real hosts.
# Real inventory.ini is git-ignored (contains actual IPs).

[web]
web1 ansible_host=REPLACE_WITH_IP

[db]
db1 ansible_host=REPLACE_WITH_IP

[all:vars]
ansible_user=ubuntu
ansible_ssh_private_key_file=~/.ssh/your_key.pem